<%# app/views/settings/weight_goals/edit.html.erb %>
<div class="max-w-md mx-auto px-4 pb-32 space-y-6">


  <h1 class="text-xl font-bold mb-4">目標体重の編集</h1>

  <%# model: [:settings, @weight_goal] と書けば、新規・編集を自動で判別してくれます %>
  <%= form_with model: [:settings, @weight_goal], local: true do |f| %>

    <div class="space-y-4 bg-base-100 p-4 rounded-lg shadow">

      <div>
        <label class="label">目標体重(kg)</label>
        <%= f.number_field :target_weight, step: 0.1, class: "input input-bordered w-full" %>
      </div>

      <div>
        <label class="label">目標日</label>
        <%= f.date_field :target_date, class: "input input-bordered w-full" %>
      </div>

      <div class="flex flex-col gap-2 mt-4">
        <%= f.submit "更新する", class: "btn btn-primary w-full h-10 text-base" %>

        <%# 編集画面なので削除ボタンを表示 %>
        <%= link_to "削除する", settings_weight_goal_path(@weight_goal),
              data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" },
              class: "btn w-full h-10 text-sm border border-red-300 text-red-400 hover:bg-red-50" %>
      </div>

    </div>
  <% end %>
  